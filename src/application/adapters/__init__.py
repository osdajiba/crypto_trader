"""Compatibility adapters between current DataFrame APIs and domain models.

Legacy adapters are intentionally not re-exported here. Code that still needs
legacy compatibility should import the adapter module directly so the dependency
is explicit at the call site.
"""

__all__: list[str] = []
